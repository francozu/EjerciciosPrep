function invertirArray(array) {
  // Invierte el arreglo array recibido por argumento.
  // Tu código:
  array.reverse();
  console.log(array);
}
var lista=[1,4,7,0,14];
invertirArray(lista);

module.exports = invertirArray;
